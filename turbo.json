{
   "$schema": "https://turborepo.org/schema.json",
   "pipeline": {
      "dev": { "outputs": [] },
      "lint": { "outputs": [] },
      "build": {
         "dependsOn": ["^build", "lint"],
         "outputs": ["build", "dist"]
      }
   }
}
